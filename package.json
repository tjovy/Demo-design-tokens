{
  "name": "@mon-entreprise/design-tokens",
  "version": "1.0.0",
  "scripts": {
    "build": "token-transformer tokens.json tokens-fixed.json && node scripts/sanitize.js && style-dictionary build --config config.json"
  },
  "devDependencies": {
    "style-dictionary": "^3.0.0",
    "token-transformer": "^0.0.33"
  }
}