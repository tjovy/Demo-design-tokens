{
  "name": "@mon-entreprise/design-tokens",
  "version": "1.0.0",
  "scripts": {
    "build": "node scripts/prepare-source.js && token-transformer tokens-build-source.json tokens-fixed.json && node scripts/sanitize.js && style-dictionary build --config config.js && rm -f tokens-build-source.json"
  },
  "devDependencies": {
    "style-dictionary": "^3.0.0",
    "token-transformer": "^0.0.33"
  }
}
